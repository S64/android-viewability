package jp.s64.android.viewablearea;

public class SystemGaps extends BaseGaps {

    public SystemGaps(
            int leftInPixels,
            int topInPixels,
            int rightInPixels,
            int bottomInPixels
    ) {
        super(
                leftInPixels,
                topInPixels,
                rightInPixels,
                bottomInPixels
        );
    }

}
